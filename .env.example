NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_MAPBOX_TOKEN=

# Protección del panel /admin (Basic Auth). NO usar el prefijo NEXT_PUBLIC_:
# deben quedar solo del lado del servidor. Si ADMIN_PASSWORD queda vacía,
# /admin no se protege.
ADMIN_USER=admin
ADMIN_PASSWORD=
